// Decision variables:
Cost = intVar(0..500);
InputErrors = intVar(0..100);
InputFileSize = intVar(0..500);

// Constraints:
InputErrors <= 1;
Cost == InputFileSize * 2;
InputFileSize <= 250;
Cost == 100;
InputFileSize == 100;

parameters {
  SearchType = DepthFirst;
}